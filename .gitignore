.claude/settings.local.json
docs/book/
guide/book/
learn/book/

# Packaged Helm charts and the consolidated installer.
dist/
# Load harness runs; the published baseline lives in test/load/baseline/.
test/load/results/

*.exe
*.exe~
*.dll
*.so
*.dylib
bin/*
Dockerfile.cross
*.test
*.out
!vendor/**/zz_generated.*
.idea
.vscode
*.swp
*.swo
*~

# Starter-template build artifacts
examples/starter-go/starter-go
# `go build ./examples/starter-go/` from the repo root drops the binary here
/starter-go
__pycache__/
*.pyc

# Stray root-built binaries. One entry per main package, so a root `go build`
# of any command cannot be committed by accident (a 9.2 MB mockprovider binary
# was, before /mockprovider was listed here).
/gateway
/manager
/mockprovider
